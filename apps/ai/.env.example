# Phase 1 only needs the basics. Phase 2 will add OPENAI_API_KEY, QDRANT_URL etc.

LOG_LEVEL=info
PORT=8000
# Origin allowed to call this service. Phase 1 = the Nest API.
ALLOWED_ORIGIN=http://localhost:3000
